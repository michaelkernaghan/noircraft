# Example proving inputs for a Yes vote
# Private inputs (kept secret)
vote = "1"              # Voting Yes
voter_secret = "12345"  # Voter's secret key

# Public inputs (visible on-chain)
proposal_id = "1"       # Proposal #1

# These are computed as: pedersen_hash([voter_secret, proposal_id])
# For now, use placeholder - in real usage, compute these values
nullifier = "0"
vote_commitment = "0"
